[package]
name = "kb-core"
version = "0.5.0"
edition = "2024"
description = "Core library for the kb knowledge-base manager"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "5"
anyhow = "1"
ignore = "0.4"
globset = "0.4"

[dev-dependencies]
tempfile = "3"
