[package]
name = "kb-core"
version = "0.1.0"
edition = "2024"
description = "Core library for the kb knowledge-base manager"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "5"
anyhow = "1"

[dev-dependencies]
tempfile = "3"
